# Board — sheleg-dev

The work-list between runs. Seeded 2026-08-19 by row **SD-01** of the cross-repository
manifesto-conformance program, which needed somewhere in *this* repository to file what it
found and did not close. Until then the only evidence file here was
`docs/evidence/verification.md`, so a deferred item had nowhere to survive a session —
the same absence `sshlg-skills` row B-30 measured about the ledger.

Priority is computed, not asserted: **`(impact × confidence) / effort`**. `impact` 1–5 on
what it costs an outside reader to be told something untrue here; `confidence` 1.0
confirmed defect · 0.7 measured but scoped · 0.4 single observation · 0.2 hypothesis;
`effort` 1–5 in engineering days including the release.

Status: `open` · `in progress` · `done` · `dropped (why)`.

| id | Priority | Item | Why it matters | Source | Status |
|---|---|---|---|---|---|
| B-79 | 5.0 | `SECURITY.md` was a wholesale copy of `seo-aeo-audit`'s, with six references this repository has nowhere | It ships in the npm tarball (`package.json` → `files`), so a reader deciding whether to trust a pack about payment credentials was told to verify it with two commands that exit 2. Manifesto M-44 (one authoritative home, references resolve) and M-07 (a claim points to an address another actor can resolve) | manifesto program row SD-01, 2026-08-19; audit evidence `SECURITY.md:10,11,17,35,54,56` | done — rewritten against measured facts; `test/validate.py` refuses a recurrence |
| B-80 | 4.0 | The PR template asked for output from `python3 test/test_page_audit.py` and named `cursor/rules/*.mdc` | Same copy, same disease, one layer out: every contributor was asked for evidence from a command that cannot run, and told to respect a directory this repository has never had. Found by the B-79 guard on its first run, not by the audit | B-79 guard, first run 2026-08-19 | done — one gate command, and the checklist item now names a rule this repo has |
| B-85 | 2.5 | `crypto-payments` had no test/live credential boundary at all | One `HELEKET_API_KEY`, which is *also* the webhook signing secret, and the document's own local-development path handed it to the reader with no environment declared beside it while calling a merchant-account toggle "test mode". So a dev, CI or agent run held the production credential and the pack never said so — in the one skill whose subject is taking money. Manifesto **M-06** (a credential that cannot reach production beats a sentence saying not to use it there) fails literally at `heleket-provider.md:126` and `:1146` | manifesto program row SD-02, 2026-08-19; audit evidence `heleket-provider.md:126`, `:1153` | done — declared `HELEKET_ENV`, a `assertHeleketEnv()` boot assertion refusing both mismatches, a pinned live merchant, the named residual exposure, and a validator check with three plants |
| B-86 | 2.0 | `stripe-billing` prescribes the same boundary and ships no assertion to copy | `references/price-integrity.md:62-64` states the house pattern (a mode variable separate from the secret, so the two can be checked against each other) and `references/testing-and-local-dev.md:210` asks the reader to confirm "something asserts they agree" — while the pack provides no such assertion anywhere. Stripe's case is *easier* than Heleket's, because `sk_test_`/`sk_live_` puts the environment in the key itself, so the snippet is a handful of lines. Same M-06 defect one skill over, and the reason `CREDENTIAL_BOUNDARIES` in `test/validate.py` is a table with one row | SD-02 measurement 2026-08-19, `price-integrity.md:62-64` and `testing-and-local-dev.md:210` | open — enforce by adding the `stripe-billing` row to `CREDENTIAL_BOUNDARIES` once the snippet exists |
| B-81 | 2.0 | `CONTRIBUTING.md` named `agent_sync.py` with no owner | A reader told to "regenerate it with `agent_sync.py setup`" cannot find the script: it ships with the `agent-sync` skill, not here. An M-07 pointer with no address — smaller than B-79 and the same shape | B-79 guard, first run 2026-08-19 | done — the sentence now names the owning skill; declared in `FOREIGN_BY_DESIGN` |
| B-82 | 1.4 | The B-79 guard does not read the 26 markdown files of the skill payload | Its corpus is the four documents *about* this repository. Inside a skill reference, 41 backticked paths name the **reader's** project (`next.config.ts`, `src/lib/heleket.ts`, `web/auth.py`) and must not resolve here — so widening needs a second rule (a repo-internal prefix such as `plugins/`, `test/`, `bin/`, `scripts/`), not a wider corpus. Until it exists, a dead `plugins/…` path inside a `SKILL.md` is caught only by the existing `references/` check | SD-01 measurement 2026-08-19: 55 non-resolving path tokens across 35 tracked markdown files, 41 of them legitimate reader-project paths | open |
| B-83 | 1.0 | `docs/AGENT_SYNC.md` names five `references/*.md` files that live in the `agent-sync` skill | Generated from `.claude/agent-sync.json`, so a fix belongs upstream in the generator, not in the file. Low impact — the paragraph says "full doctrine ships with the skill" one line earlier — but it is the same class as B-81 and a reader cannot resolve them | SD-01 measurement 2026-08-19, `docs/AGENT_SYNC.md:89-91` | open |
| B-84 | 0.9 | `docs/evidence/verification.md` REQ-001 quotes `12 checks`; the tree now prints `13` | The row measures **shipped v0.6.0** and was true of it, so it is not wrong — but nothing connects the quoted string to the validator's current output, and the same drift is what REQ-001 exists to catch one level up. The next release must re-measure it | SD-01, 2026-08-19 | open — re-measure at the next release |

| B-88 | 0.7 | `heleket-provider.md` is a size outlier among references, and SD-02 made it bigger | 1696 lines / ~18.8k tokens against a next-largest of ~4.8k (`google-signin/references/full-guide.md`) — roughly 4×. It was already the outlier at ~14.3k before this row and SD-02 added ~4.5k of credential-boundary doctrine. No rule is broken: the house 5000-token budget is enforced on `SKILL.md` bodies and `audit_skill.py` returns `0 GAP` here. But a reference this size is loaded whole, and the pack's own precedent at v0.6.0 was to **split rather than trim** when a body went over. The natural seam is sections 7–10 (the four route implementations) into a second reference | SD-02 measurement 2026-08-19, `chars/4` over every `*/references/*.md` | open — split, do not trim; both directions of the `references/` ↔ `SKILL.md` check and every in-page anchor move with it |
| B-89 | 2.5 | All four manual-gate categories were named in prose and stopped nothing | `manifesto.md:204` puts money movement, irreversible action, production access and destructive operations on the authorised person's side — "the agent prepares the decision and its evidence" — and `:200` says a precondition is stronger than a warning. This pack said **"Never auto-refund from the webhook. Route holds and refunds to a queue a human can see"** (`crypto-payments/SKILL.md:310`) and **"route it to a human — evidence has a deadline"** (`stripe-billing/references/webhook-events.md:170`) while shipping no hooks, no permission list and no gate. SD-02's boot assertion was the one real control and it runs inside the reader's application, so a shell that merely **exports** the live merchant credential never reaches it | manifesto program row SD-03, 2026-08-19; audit evidence `crypto-payments/SKILL.md:309-310`, `webhook-events.md:169-170` | done — a `PreToolUse` hook with eight refusals, a pure decision module, 65 two-directional fixtures, `check_manual_gate()` in the validator and eight negative self-tests |
| B-92 | 1.6 | The hook has never fired in a live Claude Code session | Everything measured about it is the module (65 fixtures), the script (run as a process over five payload shapes) and the manifest (`claude plugin validate --strict` schema-validates `hooks/hooks.json` — proved by planting `NotAnEvent`, which it refuses). What is **not** measured is the harness contract: that a plugin's `PreToolUse` hook fires on a `Bash` call when the plugin is enabled, that a `deny` decision actually blocks the tool, and that a variable exported *inside* a tool call does not reach the hook's environment — the last is the reason `SHELEG_DEV_LIVE_AUTHORISED` is an env var rather than a file, and the `self-authorisation` refusal exists so the gate holds at the payload layer even if that assumption is wrong | SD-03, 2026-08-19 — stated as unmeasured | open — confirm in a session where the plugin is installed, then record the observation |
| B-90 | 1.5 | The skills-copy install channels carry no hook, and nothing enforces registering one | `npx @ssheleg/sheleg-dev`, `install.sh` and `npx skills add` copy the six skill directories into `~/.claude/skills/` and stop there — the gate lives at plugin level and travels only with the plugin. `README.md` → *The manual gate* carries the settings snippet and the installer prints a reminder, but **a printed reminder is a warning, which is the exact thing M-30 says is weaker than a precondition.** Writing to a person's `~/.claude/settings.json` is deliberately not done: it is a file the operator owns and did not write, with no version control behind it, and the family umbrella carries two defects in its own history from doing it | SD-03, 2026-08-19 | open — the honest fixes are a `--with-hook` flag that asks first, or dropping the copy channels in favour of the plugin |
| B-91 | 1.0 | The gate knows two providers; three credential-holding skills have no entry | `NO_TEST_VARIANT` carries one row (`HELEKET_API_KEY`) and the live-key shape rule covers Stripe, because Stripe stamps the environment into the key. `google-auth`, `google-signin` and `ad-tracking` hand the reader service-account keys, OAuth client secrets and CAPI access tokens, and **none of them is in the table** — so exporting a production service-account key in a run declaring `test` is not refused. Each row needs the provider's credential model established from the documentation first, the way SD-02 did for Heleket; guessing a prefix would ship a rule that refuses the wrong thing. Sibling of B-87, which is the documentation half of the same gap | SD-03, 2026-08-19 — measured absence, not assumed | open — one row per provider, each with a fixture in both directions |
| B-93 | 2.0 | `SECURITY.md` states counted numbers and nothing recomputes them | This change moved four of them (27 → 30 payload files, 33 → 36 tarball files, one → four non-markdown lines, plus a new fixture count). Every one was re-measured by running the command, and every one is now a literal in a document that the validator checks only for *path resolution*, not for arithmetic. Same class as B-84 one document over, and the reason `docs/evidence/verification.md` REQ-001 exists | SD-03, 2026-08-19 | open — either a validator check that recomputes each quoted count, or the counts move into the command's own output and out of the prose |
| B-94 | 2.5 | The four money invariants were prose that delegated enforcement to the reader | `manifesto.md:200` is that a test is stronger than an instruction, and `:289` that evidence proves no more than it observed. This pack stated all four invariants whose failure is money rather than an error page — the webhook is the payment (`ad-tracking/SKILL.md:268`), one `event_id` on both sides (`:264`), a cumulative refund total (`stripe-billing/references/webhook-events.md:163`) and unordered delivery (`:183`) — every citation read at `90e9621`, the tree the audit read — and shipped no fixture for any of them. The giveaway the audit quoted was in the testing reference itself: *"For every guard, delete it and re-run"* (`testing-and-local-dev.md:157-172`), an instruction to perform the mutation testing in the document whose subject is proving a money defect would be caught | manifesto program row SD-04, 2026-08-19; audit evidence at `90e9621`: `stripe-billing/references/testing-and-local-dev.md:157-172`, `ad-tracking/SKILL.md:264-296` | done — two `fixtures/` directories (14 provider payloads), two assertion packs (18 assertions), a `--self-test` that isolates all 14 rules, `check_money_fixtures()` in the validator, `test/fixtures_test.js`, and five negative self-tests |
| B-95 | 1.7 | `crypto-payments/SKILL.md` is past the house working limit at ~4894 tokens | `audit_skill.py --house` reports `1 GAP, 13 PASS`: inside the 5000 Agent Skills budget and past the 4750 working limit, so **the next section breaches it**. Pre-existing — measured on the SD-03 tree with `git diff HEAD -- plugins/sheleg-dev/skills/crypto-payments` returning nothing for this row — and named rather than left for the run that discovers it by being blocked. The house answer at that point is a split, not a trim, and the natural seam is the same one B-88 names for `heleket-provider.md` | SD-04 measurement 2026-08-19, `python3 audit_skill.py --house plugins/sheleg-dev/skills/crypto-payments` | open — split before the next addition; the other five skills return `0 GAP` |
| B-96 | 1.5 | A new invariant claimed in a reference with no fixture cannot be detected | The guard checks that every invariant the **manifest** claims has a fixture, and that every phrase the manifest records is still in its document. It cannot see a *new* sentence stating a fifth money invariant, because "this paragraph states an invariant" is not a machine-decidable property of prose. So the both-directions claim is bounded: it holds over the enumerated set and says nothing about additions. Same shape as `FOREIGN_BY_DESIGN` — an enumeration is honest where a rule would guess | SD-04, 2026-08-19 — stated as unmeasurable rather than claimed | open — the honest fix is a review habit (a new money invariant adds a manifest row in the same change), not a wider regex |
| B-97 | 1.2 | Two assertion-pack runners are duplicated, one per skill | `assert-money-invariants.mjs` and `assert-dedup-contract.mjs` each carry their own ~80-line runner and isolation reporter. Deliberate: a reader installs one skill, so a shared import would resolve to nothing in the common case — the `LINK_ESCAPE` problem `audit_skill.py` warns about, one file type over. But it is still two homes for one mechanism, and the stripe one has since grown multi-rule mutant support the ad-tracking one does not have. A drift, not a defect, and it will become one | SD-04, 2026-08-19 | open — either keep them deliberately divergent and say so in both files, or ship the runner as a third file both copy |
| B-87 | 0.6 | The other three credential-holding skills were not audited for a test/live boundary | `google-auth`, `google-signin` and `ad-tracking` all hand the reader secrets (service-account keys, OAuth client secrets, CAPI access tokens). SD-02 scoped itself to `crypto-payments` and did not look, so their state is unknown rather than clean — and an unmeasured skill reads as a passing one, which is the reading `docs/evidence/verification.md` exists to break | SD-02, 2026-08-19 — not measured, stated as unmeasured | open |

## How this file is used

A run reads it before starting and quotes the open count. Anything a run finds and does not
close gets an id here with a computed priority, or it did not happen. A row that leaves
without either a `done` or a stated reason is what this file exists to prevent.
